<h1><%= @playlist.title %></h1>

<h3>Songs</h3>
<ul>
	<% @songs.each do |song| %>
		<li><%= song.title %></li>
		<%= button_to "Remove from Playlist", 
				playlist_songs_url(playlist_id: @playlist.id, song_id: song.id), method: :delete %>
	<% end %>
</ul>   